import 'package:flutter/material.dart';

class StyledTextTemp extends StatelessWidget {
  @override
  Widget build(context) {
    // TODO: implement build
    return const Text(
      'Hello World',
      style: TextStyle(color: Colors.white, fontSize: 30),
    );
  }
}
